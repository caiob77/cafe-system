# Print-agent placeholder (sem impressora física ainda).
# Para usar: gere um token via POST /api/v1/printer-devices
# ou use o token fixo do seed em ambiente local.

API_BASE_URL=http://localhost:3333
API_WS_URL=ws://localhost:3333/api/v1/realtime

# Em produção, use obrigatoriamente https:// e wss://.
PRINTER_TOKEN=
